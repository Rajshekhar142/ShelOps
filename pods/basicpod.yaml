apiVersion: v1

kind: Pod

metadata:
  name: nginx-pod
  labels:
    app: nginx

spec:
# defines the desired state of the pod
  containers:
    - name: nginx
      image: nginx:latest
      ports:
      - containerPort: 80
